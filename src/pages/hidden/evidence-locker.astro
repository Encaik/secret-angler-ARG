---
// /hidden/evidence-locker — 证据管理（待删除数据队列 + 手机举报界面）
import HiddenLayout from '../../components/HiddenLayout.astro';
const BASE = __BASE_PATH__;
---

<HiddenLayout title="证据管理">
  <div class="breadcrumb">
    <a href={`${BASE}hidden/admin/`} target="_blank">管理后台</a><span>›</span>
    证据管理
  </div>

  <h2 style="color: #2c1810; margin-bottom: 4px;">证据管理 · 待删除队列</h2>
  <p class="text-muted" style="margin-bottom: 16px;">待清理证据 · 自动删除：每48小时 · 下次清理：2026-06-06 00:00</p>

  <div class="hidden-warning">⚠ 以下项目已排入自动删除队列。下次清理：2026-06-06 00:00。删除后数据不可恢复。</div>

  <!-- 待删除证据列表 -->
  <div class="card" style="margin-bottom: 12px; border-left: 3px solid #a0702c;">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">
      <h4 style="margin-bottom: 0;">待删除·甲辰004-001</h4>
      <span class="hidden-badge amber">待删除</span>
    </div>
    <p style="font-size: 0.78rem; color: #9b8c7a; margin-bottom: 6px;">
      来源：用户区 · 作者：地下三尺 · 标记时间：2026-06-04 23:00
    </p>
    <p style="font-size: 0.84rem; color: #4a3728;">
      <strong>内容：</strong>坐标交叉比对完整数据集（含8处重合点GPS、现场照片水印、时间窗口分析）。
    </p>
    <p style="font-size: 0.78rem; color: #5c2e2e;">删除原因：含有可直接指证组织运作模式的证据材料。</p>
  </div>

  <div class="card" style="margin-bottom: 12px; border-left: 3px solid #a0702c;">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">
      <h4 style="margin-bottom: 0;">待删除·甲辰004-002</h4>
      <span class="hidden-badge amber">待删除</span>
    </div>
    <p style="font-size: 0.78rem; color: #9b8c7a; margin-bottom: 6px;">
      来源：用户区 · 作者：signal_屿 · 标记时间：2026-06-04 23:00
    </p>
    <p style="font-size: 0.84rem; color: #4a3728;">
      <strong>内容：</strong>凤凰山1420MHz信号频谱分析原始数据 + 设备推测记录 + 47处灯位热力图分布分析。
    </p>
    <p style="font-size: 0.78rem; color: #5c2e2e;">
      删除原因：技术分析已精确到可定位信号灯的程度。若被执法部门获取可逆向追踪信号源。
    </p>
  </div>

  <div class="card" style="margin-bottom: 12px; border-left: 3px solid #a0702c;">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">
      <h4 style="margin-bottom: 0;">待删除·甲辰004-003</h4>
      <span class="hidden-badge amber">待删除</span>
    </div>
    <p style="font-size: 0.78rem; color: #9b8c7a; margin-bottom: 6px;">来源：沈辞私信 · 标记时间：2026-06-04 23:45</p>
    <p style="font-size: 0.84rem; color: #4a3728;">
      <strong>内容：</strong
      >沈辞与林屿2026年5月全部私信记录（含坐标比对讨论、秦岭布景验证、教务系统密码传递、仅自己可见帖文的提示）。
    </p>
    <p style="font-size: 0.78rem; color: #5c2e2e;">
      删除原因：私信记录显示二人已确认论坛的系统性造假。若被林屿截屏保存将不可挽回。
    </p>
  </div>

  <div class="card" style="margin-bottom: 12px; border-left: 3px solid #5c2e2e;">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">
      <h4 style="margin-bottom: 0;">待删除·甲辰004-004</h4>
      <span class="hidden-badge maroon">待删除</span>
    </div>
    <p style="font-size: 0.78rem; color: #9b8c7a; margin-bottom: 6px;">来源：沈辞账号 · 标记时间：2026-06-04 23:50</p>
    <p style="font-size: 0.84rem; color: #4a3728;">
      <strong>内容：</strong>沈辞仅自己可见帖文"关于贺兰山之行的一些补充笔记"（权限等级255）。
    </p>
    <p style="font-size: 0.78rem; color: #5c2e2e;">
      删除原因：监院称"内容看起来是普通散记"，但首座怀疑其中藏有加密信息。权限过高，需数据库管理员直接操作。<strong
        >删除已排程：2026-06-04 01:00执行。</strong
      >
    </p>
  </div>

  <!-- 已删除记录 -->
  <div class="card" style="margin-bottom: 12px; opacity: 0.6;">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">
      <h4 style="margin-bottom: 0;">已删除·甲辰005-001</h4>
      <span class="hidden-badge muted">已删除</span>
    </div>
    <p style="font-size: 0.78rem; color: #9b8c7a; margin-bottom: 6px;">删除时间：2026-05-26 13:00 · 操作人：首座</p>
    <p style="font-size: 0.84rem; color: #9b8c7a;">
      <strong>内容：</strong>贺兰山引导帖3篇 + 引导评论12条。已从论坛完全清除。
    </p>
  </div>

  <div class="card" style="margin-bottom: 12px; opacity: 0.6;">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">
      <h4 style="margin-bottom: 0;">已删除·甲辰005-002</h4>
      <span class="hidden-badge muted">已删除</span>
    </div>
    <p style="font-size: 0.78rem; color: #9b8c7a; margin-bottom: 6px;">删除时间：2026-05-28 09:00 · 操作人：首座</p>
    <p style="font-size: 0.84rem; color: #9b8c7a;">
      <strong>内容：</strong>外联人员在第247号帖文下的原始引导评论（"有个冷门地方推荐给你"）。替换为普通关心评论。
    </p>
  </div>

  <!-- ========== 操作区 ========== -->
  <div style="margin-top: 24px; text-align: center;">
    <button
      id="open-phone-btn"
      style="padding: 10px 28px; background: #c0392b; color: #fff; border: none; border-radius: 3px; cursor: pointer; font-size: 0.85rem; letter-spacing: 2px;"
    >
      📞 匿名举报
    </button>
    <p style="font-size: 0.68rem; color: #b0a090; margin-top: 8px;">通过加密通道提交证据至公安机关。操作不可撤销。</p>
  </div>

  <!-- ========== 手机弹窗 ========== -->
  <div
    id="phone-modal"
    style="display:none;position:fixed;top:0;left:0;width:100%;height:100%;background:rgba(0,0,0,0.75);z-index:9999;align-items:center;justify-content:center;"
  >
    <div
      style="display:inline-block;background:#1a1a1a;border-radius:16px;padding:12px 8px 16px;box-shadow:0 4px 32px rgba(0,0,0,0.5);max-width:280px;"
    >
      <!-- 屏幕 -->
      <div
        id="phone-screen"
        style="background:#f5f5f5;border-radius:4px;padding:20px 16px;min-height:280px;font-family:-apple-system,'PingFang SC',sans-serif;"
      >
        <!-- 默认拨号界面 -->
        <div id="phone-dialer">
          <div style="font-size:0.7rem;color:#999;margin-bottom:6px;text-align:center;">加密举报通道</div>
          <div style="font-size:0.6rem;color:#bbb;margin-bottom:16px;text-align:center;">
            已收集的证据将随举报一并提交
          </div>
          <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:12px;">
            <button
              class="phone-action-btn"
              data-action="report"
              style="padding:10px;background:#c0392b;color:#fff;border:none;border-radius:6px;cursor:pointer;font-size:0.75rem;"
              >📞 报警举报</button
            >
            <button
              class="phone-action-btn"
              data-action="ticket"
              style="padding:10px;background:#1677ff;color:#fff;border:none;border-radius:6px;cursor:pointer;font-size:0.75rem;"
              >🎫 购买车票</button
            >
          </div>
          <div style="font-size:0.6rem;color:#bbb;text-align:center;">匿名加密通道。公安机关无法追溯举报人身份。</div>
        </div>

        <!-- 结果界面（JS注入） -->
        <div id="phone-result" style="display:none;"></div>
      </div>
      <!-- 底部按钮 -->
      <div style="width:32px;height:32px;border-radius:50%;border:2px solid #444;margin:8px auto 0;"></div>
    </div>
  </div>
</HiddenLayout>

<script src="../../scripts/ending.ts"></script>
<script>
  const BASE = window.__BASE__ || '/';

  function getEngine() {
    return (window as any).__endingEngine;
  }

  function calcScore() {
    const engine = getEngine();
    if (engine) {
      const result = engine.evaluateEnding();
      const progress = JSON.parse(localStorage.getItem('arg_progress') || '{}');
      const visited: string[] = progress.discoveredPages || [];
      const hasDeadDrop = visited.some((v: string) => v.includes('/hidden/dead-drop'));
      return { score: result.score, hasDeadDrop, shenciSaved: engine.isShenciSaved(result.score) };
    }
    return { score: 0, hasDeadDrop: false, shenciSaved: false };
  }

  function getEnding(score: number, hasDeadDrop: boolean): number {
    const engine = getEngine();
    if (engine) return engine.evaluateEnding().ending;
    let ending: number;
    if (score >= 18) ending = 4;
    else if (score >= 12) ending = 3;
    else if (score >= 7) ending = 2;
    else ending = 1;
    if (hasDeadDrop && score >= 12 && ending < 4) ending = 4;
    return ending;
  }

  function renderPhoneResult(score: number, shenciSaved: boolean, ending: number, action: string) {
    const resultEl = document.getElementById('phone-result');
    if (!resultEl) return;

    const lowScore = score < 7;
    const mediumScore = score >= 7 && score < 12;

    let html = '';

    if (action === 'ticket') {
      // 买票 → 自己去 → 结局1
      html = `
        <div style="text-align:center;">
          <div style="font-size:0.8rem;color:#1677ff;font-weight:bold;margin-bottom:12px;">🚄 铁路12306</div>
          <div style="font-size:0.68rem;color:#666;margin-bottom:10px;text-align:left;line-height:1.7;">
            你关掉了证据页面，打开了12306。<br/><br/>
            贺兰山 → 银川。2026年6月7日。硬座。¥112.00。<br/><br/>
            沈辞的笔记里写过"只查外围，不深入深山禁区"。你没听他的。
          </div>
          <div style="background:#e6f4ff;padding:8px;border-radius:4px;font-size:0.65rem;color:#555;margin-bottom:8px;">
            订单号：E84903216<br/>
            状态：<span style="color:#1677ff;">已出票</span>
          </div>
        </div>`;
    } else if (lowScore) {
      // 证据严重不足 → 警方无法行动
      html = `
        <div style="text-align:center;">
          <div style="font-size:0.8rem;color:#c0392b;font-weight:bold;margin-bottom:12px;">📞 110 报警电话</div>
          <div style="background:#111;color:#4a4;padding:6px 10px;border-radius:4px;font-size:0.62rem;text-align:left;margin-bottom:10px;line-height:1.6;">
            [通话录音 2026-06-05]<br/>
            接警员：110，请讲。<br/>
            你：我要举报一个犯罪组织……但我手头的证据不够完整。<br/>
            接警员：请提供具体地点和证据材料。<br/>
            你：……我只有一些零散的截图和代号。没有具体坐标。<br/>
            接警员：没有具体地点和实质性证据，我们无法立案。建议你先收集更多材料。<br/>
            <span style="color:#888;">[通话结束 · 时长 01:47]</span>
          </div>
          <div style="font-size:0.65rem;color:#999;">你手上的东西不足以让警方介入。你只能自己去——或者永远不去。</div>
        </div>`;
    } else if (mediumScore && !shenciSaved) {
      // 证据部分但不充分 → 警方需要时间取证 → 沈辞没赶上
      html = `
        <div style="text-align:center;">
          <div style="font-size:0.8rem;color:#c0392b;font-weight:bold;margin-bottom:12px;">📞 110 报警电话</div>
          <div style="background:#111;color:#4a4;padding:6px 10px;border-radius:4px;font-size:0.62rem;text-align:left;margin-bottom:10px;line-height:1.6;">
            [通话录音 2026-06-05]<br/>
            接警员：110，请讲。<br/>
            你：我要举报鄂西一个废弃矿洞里关着人。坐标31.12，111.45。我手上有内部文件和通讯记录。<br/>
            接警员：请保持电话通畅，我为你转接刑侦支队。<br/><br/>
            [加密举报平台 · 2026-06-05]<br/>
            举报编号：HB-2026-0605-0047<br/>
            状态：<span style="color:#f0a030;">已受理 · 证据核查中</span><br/>
            预计完成取证：6-10个工作日<br/><br/>
            <span style="color:#c0392b;">6月8日，外院医生飞抵基地。甲辰003配型确认。</span>
          </div>
          <div style="font-size:0.65rem;color:#999;">警方最终突袭了基地。另外三人获救。沈辞没赶上。</div>
        </div>`;
    } else {
      // 证据充分 → 警方立即行动 → 沈辞得救
      const fullChain = score >= 18;
      html = `
        <div style="text-align:center;">
          <div style="font-size:0.8rem;color:#c0392b;font-weight:bold;margin-bottom:12px;">📞 ${fullChain ? '公安部专案组热线' : '省公安厅网安总队'}</div>
          <div style="background:#111;color:#4a4;padding:6px 10px;border-radius:4px;font-size:0.62rem;text-align:left;margin-bottom:10px;line-height:1.6;">
            [举报平台 · 加密通道 · 2026-06-05]<br/>
            举报编号：${fullChain ? 'GA-2026-0605-0012' : 'HB-2026-0605-0047'}<br/>
            提交材料：${fullChain ? '完整证据链（含资金流水、基地坐标、医疗合作名单、全部钓鱼站点数据）+ 郑义自动举报脚本已同步接收' : '暗网交易记录 + 基地坐标 + 作案流程文件'}<br/><br/>
            ${
              fullChain
                ? '状态：<span style="color:#4a4;">已立案 · 专案组成立 · 即刻出警</span><br/>证据链完整。坐标精确到十米级。受害者身份全部核实。6月5日当晚突袭。'
                : '状态：<span style="color:#4a4;">已立案 · 即刻出警</span><br/>坐标明确，证据充分。6月5日当晚跨省联合行动。'
            }<br/><br/>
            <span style="color:#4a4;">抵达时间：6月5日 23:40。甲辰003（沈辞）尚在体检区，未进入手术排程。</span>
          </div>
          <div style="font-size:0.65rem;color:#999;">${fullChain ? '全链覆灭。全部受害者获救。' : '基地被捣毁。全部受害者获救。部分涉案人员在逃。'}</div>
        </div>`;
    }

    html += `
      <div style="margin-top:14px;">
        <button id="ending-proceed-btn" style="padding:8px 24px;background:#333;color:#fff;border:none;border-radius:4px;cursor:pointer;font-size:0.75rem;">
          继续
        </button>
      </div>`;

    resultEl.innerHTML = html;

    document.getElementById('ending-proceed-btn')?.addEventListener('click', () => {
      const progress = JSON.parse(localStorage.getItem('arg_progress') || '{}');
      progress.ending = ending;
      progress.evidenceScore = score;
      localStorage.setItem('arg_progress', JSON.stringify(progress));
      window.location.href = BASE + 'ending/' + ending + '/';
    });
  }

  function showResult(action: 'report' | 'ticket') {
    const { score, hasDeadDrop, shenciSaved } = calcScore();
    const ending = getEnding(score, hasDeadDrop);
    const finalEnding = action === 'ticket' ? 1 : ending;
    const finalSaved = action === 'ticket' ? false : shenciSaved;

    document.getElementById('phone-dialer')!.style.display = 'none';
    document.getElementById('phone-result')!.style.display = 'block';

    renderPhoneResult(action === 'ticket' ? 1 : score, finalSaved, finalEnding, action);
  }

  document.addEventListener('DOMContentLoaded', () => {
    const modal = document.getElementById('phone-modal');
    const openBtn = document.getElementById('open-phone-btn');

    // 打开弹窗
    openBtn?.addEventListener('click', () => {
      if (modal) modal.style.display = 'flex';
    });

    // 点击遮罩关闭
    modal?.addEventListener('click', (e) => {
      if (e.target === modal) modal.style.display = 'none';
    });

    // 绑定手机按钮
    document.querySelectorAll('.phone-action-btn').forEach((btn) => {
      btn.addEventListener('click', () => {
        const action = (btn as HTMLElement).dataset.action as 'report' | 'ticket';
        showResult(action);
      });
    });
  });
</script>
