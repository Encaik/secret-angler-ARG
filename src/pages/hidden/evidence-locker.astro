---
// /hidden/evidence-locker — 证据缓存区
// 团伙用于反侦察的证据销毁缓存。包含即将被清理的关键数据。
import HiddenLayout from '../../components/HiddenLayout.astro';
const BASE = __BASE_PATH__;
---

<HiddenLayout title="证据缓存">
  <div class="breadcrumb">
    <a href={`${BASE}hidden/admin/`} target="_blank">后台管理</a><span>›</span>
    证据缓存
  </div>

  <h2 style="color: #c0392b; margin-bottom: 4px;">🔒 证据缓存区</h2>
  <p class="text-muted" style="margin-bottom: 16px;">用于集中存放待销毁的敏感数据 · 定时清理：每48小时 · 下次清理：2026-06-06 00:00</p>

  <div class="hidden-warning">
    ⚠ 此缓存区包含即将被永久删除的数据。如果你看到这些内容——说明你赶在了清理程序之前。请尽快保存。
  </div>

  <!-- 缓存项列表 -->
  <div class="card" style="margin-bottom: 12px; border-left: 3px solid #e67e22;">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">
      <h4 style="margin-bottom: 0;">📦 缓存项 #DEL-2026-0604-001</h4>
      <span class="hidden-badge" style="background:#e67e22;">待删除</span>
    </div>
    <p style="font-size: 0.78rem; color: #888; margin-bottom: 6px;">来源：会员区 · 创建者：地下三尺 · 标记时间：2026-06-04 00:05</p>
    <p style="font-size: 0.84rem; color: #aaa;">
      <strong>内容：</strong>坐标交叉比对完整数据集（含8个重合点位的GPS坐标、现场照片GPS水印数据、时间窗口分析）。
    </p>
    <p style="font-size: 0.78rem; color: #c0392b;">
      删除原因：包含可直接指证"深海裂隙"项目运作模式的证据。
    </p>
  </div>

  <div class="card" style="margin-bottom: 12px; border-left: 3px solid #e67e22;">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">
      <h4 style="margin-bottom: 0;">📦 缓存项 #DEL-2026-0604-002</h4>
      <span class="hidden-badge" style="background:#e67e22;">待删除</span>
    </div>
    <p style="font-size: 0.78rem; color: #888; margin-bottom: 6px;">来源：会员区 · 创建者：signal_屿 · 标记时间：2026-06-04 00:05</p>
    <p style="font-size: 0.84rem; color: #aaa;">
      <strong>内容：</strong>凤凰山1420MHz信号频谱分析原始数据 + 设备推测报告 + 47个点位的热力图分布分析。
    </p>
    <p style="font-size: 0.78rem; color: #c0392b;">
      删除原因：技术分析精确到了可定位发射设备的程度。如被执法部门获取可逆向追踪信号源。
    </p>
  </div>

  <div class="card" style="margin-bottom: 12px; border-left: 3px solid #e67e22;">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">
      <h4 style="margin-bottom: 0;">📦 缓存项 #DEL-2026-0604-003</h4>
      <span class="hidden-badge" style="background:#e67e22;">待删除</span>
    </div>
    <p style="font-size: 0.78rem; color: #888; margin-bottom: 6px;">来源：三尺私信记录 · 标记时间：2026-06-04 00:08</p>
    <p style="font-size: 0.84rem; color: #aaa;">
      <strong>内容：</strong>沈辞与林屿2026年5月全部私信记录（含坐标比对讨论、秦岭假点位验证、教务系统密码传递、仅自己可见帖文的提示）。
    </p>
    <p style="font-size: 0.78rem; color: #c0392b;">
      删除原因：私信记录显示两人已确认导航站的系统性造假。如被signal_屿截图保存将无法挽回。
    </p>
  </div>

  <div class="card" style="margin-bottom: 12px; border-left: 3px solid #c0392b;">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">
      <h4 style="margin-bottom: 0;">📦 缓存项 #DEL-2026-0604-004</h4>
      <span class="hidden-badge">待删除</span>
    </div>
    <p style="font-size: 0.78rem; color: #888; margin-bottom: 6px;">来源：三尺账号 · 标记时间：2026-06-04 00:12</p>
    <p style="font-size: 0.84rem; color: #aaa;">
      <strong>内容：</strong>沈辞仅自己可见帖文"关于贺兰山之行的一些补充笔记"（阅读权限255）。
    </p>
    <p style="font-size: 0.78rem; color: #c0392b;">
      删除原因：技术_B 报告称"内容看起来是普通散文"，但运营_A 怀疑其中藏有加密信息。权限过高，需数据库root直接操作。<strong>删除操作已排程：2026-06-04 03:00 执行。</strong>
    </p>
  </div>

  <!-- 已删除项 -->
  <div class="card" style="margin-bottom: 12px; opacity: 0.6;">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">
      <h4 style="margin-bottom: 0;">📦 缓存项 #DEL-2026-0526-001</h4>
      <span class="hidden-badge" style="background:#888;">已删除</span>
    </div>
    <p style="font-size: 0.78rem; color: #666; margin-bottom: 6px;">删除时间：2026-05-26 14:30 · 操作员：运营_A</p>
    <p style="font-size: 0.84rem; color: #666;">
      <strong>内容：</strong>贺兰山相关推荐帖文3篇 + 引导性评论12条。已从站点前台完全移除。
    </p>
  </div>

  <div class="card" style="margin-bottom: 12px; opacity: 0.6;">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">
      <h4 style="margin-bottom: 0;">📦 缓存项 #DEL-2026-0528-001</h4>
      <span class="hidden-badge" style="background:#888;">已删除</span>
    </div>
    <p style="font-size: 0.78rem; color: #666; margin-bottom: 6px;">删除时间：2026-05-28 09:15 · 操作员：运营_A</p>
    <p style="font-size: 0.84rem; color: #666;">
      <strong>内容：</strong>山野探秘人在247号帖下的原始引导评论（"有个冷门点位推荐给你"）。替换为正常关心评论。
    </p>
  </div>

  <!-- 操作区 —— 导出证据并触发结局-->
  <div style="margin-top:20px;padding:16px;background:#0d0d0d;border:1px solid #1a1a1a;text-align:center;">
    <p style="font-size:0.78rem;color:#666;margin-bottom:10px;">
      以上缓存项尚未被系统自动清理。导出全部数据并匿名提交至执法部门。
    </p>
    <button id="submit-evidence-btn" style="padding:10px 32px;background:#1a0a0a;color:#8b3a3a;border:1px solid #2a1a1a;cursor:pointer;font-size:0.85rem;">
      导出全部证据 · 匿名提交
    </button>
    <p id="submit-status" style="font-size:0.7rem;color:#555;margin-top:8px;display:none;"></p>
  </div>
</HiddenLayout>

<script>
  const BASE = window.__BASE__ || '/';

  // 证据评分映射
  const EVIDENCE = {
    '/trigger/rift':      { weight: 2 },
    '/trigger/stargate':  { weight: 2 },
    '/trigger/base':      { weight: 3 },
    '/hidden/darknet':    { weight: 3 },
    '/hidden/board':      { weight: 2 },
    '/hidden/operation':  { weight: 2 },
    '/hidden/locations':  { weight: 3 },
    '/hidden/dead-drop':  { weight: 4 },
  };

  document.getElementById('submit-evidence-btn').addEventListener('click', function() {
    const progress = JSON.parse(localStorage.getItem('arg_progress') || '{}');
    const visited = progress.discoveredPages || [];
    let score = 0;
    let hasDeadDrop = false;

    for (const [page, ev] of Object.entries(EVIDENCE)) {
      const found = visited.some(function(v) {
        return v === page || v === page + '/' || v.replace(/\/$/, '') === page;
      });
      if (found) {
        score += ev.weight;
        if (page === '/hidden/dead-drop') hasDeadDrop = true;
      }
    }

    // 判定结局
    let ending;
    if (score >= 18) ending = 4;
    else if (score >= 12) ending = 3;
    else if (score >= 7) ending = 2;
    else ending = 1;

    // 有举报脚本+分数≥12 → 升级结局4
    if (hasDeadDrop && score >= 12 && ending < 4) ending = 4;

    // 保存结局到进度
    progress.ending = ending;
    progress.evidenceScore = score;
    localStorage.setItem('arg_progress', JSON.stringify(progress));

    // 跳转到对应结局
    window.location.href = BASE + 'ending/' + ending + '/';
  });
</script>
